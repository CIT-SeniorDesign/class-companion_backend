plugins:
  - serverless-offline

service: notify-subscription-microservice

provider:
  name: aws
  runtime: nodejs14.x
  stage: dev
  region: us-east-1

functions:
  subscribe:
    handler: subscribe.handler
    events:
      - httpApi:
          path: /subscribe
          method: post